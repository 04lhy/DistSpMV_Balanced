# DistSpMV_Balanced - Python dependencies (plotting only)
# The C++ binary is built with CMake + MPI + OpenMP (see README).
# Python 3.8+ required for the experiment visualisation script.

numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
